# ASGI with WebSocket support (use this for production with Channels)
web: daphne -b 0.0.0.0 -p $PORT config.asgi:application

# Alternative: Uvicorn (also supports ASGI)
# web: uvicorn config.asgi:application --host 0.0.0.0 --port $PORT --workers 4

# Legacy WSGI (fallback if Redis not available)
# web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT

